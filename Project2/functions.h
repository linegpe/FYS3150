#ifndef FUNCTIONS_H
#define FUNCTIONS_H

// Function used to print matrix. 
// Not used in final program, but very
// usefull while writing or editing main.cpp
void printMatrix(double ** A, int N);

// Function used to sort the values of a vector, 
// from lowest to highest value
double* sort(double *v, int n, double** R);


//void makeAmatrix(int N, double h, double *V, double nondiagonal_value);
//void makeRmatrix(int N);

#endif